# Code owners for platform-sdk-swift.
#
# The `Stable Main` ruleset has `require_code_owner_review: true`, but with no
# CODEOWNERS file no path had an owner, so the rule matched nothing. This file
# is what turns it on: every PR now needs an approval from someone listed here.
#
# Ownership is repo-wide because the list given is repo-wide (Jeff Hampton and
# David Fedor, 2026-09-23). To narrow it, add more specific patterns below.
# For a given file the last matching rule wins, not the union of matches, so a
# narrower rule replaces these owners rather than adding to them.

*   @jhampton @davidfedor
